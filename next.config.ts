import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["tirony.me", "images.squarespace-cdn.com", "th.bing.com"], // Add the domain of your image
  },
};

export default nextConfig;
